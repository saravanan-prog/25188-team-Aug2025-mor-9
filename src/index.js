import React from 'react';
import ReactDOM from 'react-dom/client';
import SearchBar from './EmployeeSearch/SearchBar';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SearchBar/>
  </React.StrictMode>
);
